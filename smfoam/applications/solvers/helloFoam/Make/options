# =========================================================================
# 1. INCLUDES (Onde achar o helloSolver.H)
# =========================================================================
EXE_INC = \
    -I$(LIB_SRC)/OpenFOAM/lnInclude \
    -I$(SMF_MODULES)/helloSolver

# =========================================================================
# 2. LIBRARIES (Onde achar as pastas -L e quais binarios puxar -l)
# =========================================================================
EXE_LIBS = \
    -lOpenFOAM \
    -L$(SMF_LIBBIN) \
    -lhelloSolver \
    -lhelloLib
